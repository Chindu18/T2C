import type React from "react"
import type { Metadata } from "next"
import { Playfair_Display, Inter } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
})

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
})

export const metadata: Metadata = {
  title: "T2C - The Chennai Cuisine | Authentic Tamil Food in Sweden",
  description:
    "Experience the authentic flavors of Chennai at T2C - The Chennai Cuisine in Södertalje, Sweden. T2C From the Hearts of South India.",
  keywords: "Chennai cuisine, Tamil food, Indian restaurant Sweden, Södertalje restaurant, authentic South Indian food",
  openGraph: {
    title: "T2C - The Chennai Cuisine",
    description: "Authentic Chennai flavors in the heart of Sweden",
    type: "website",
  },
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable}`}>
      <body className="font-sans antialiased">
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
